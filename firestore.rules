rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /fitnessPlans/{userId} {
      // Allow users to read and write their own fitness plan
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
